version: "3"

services:
  postgres:
    image: "postgres"
    ports:
      - "5432:5432"
    env_file:
      - ./.env
    volumes:
      - postgres-data:/var/lib/postgresql/data/
    command: ["postgres", "-c", "log_statement=all"]

volumes:
  postgres-data:
